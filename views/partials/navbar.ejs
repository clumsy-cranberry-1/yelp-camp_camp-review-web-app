<nav class="navbar navbar-expand-md navbar-light sticky-top bg-light">
    <div class="vw-100 container-fluid py-2 px-4">
        <a class="navbar-brand p-0" href="/">
            <img src="/images/logo-light.png" alt="logo" width="168" height="auto">&nbsp;
        </a>
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon small"></span>
        </button>
        <div class="collapse navbar-collapse mt-4 m-md-0 small" id="navbarNav">
            <div class="navbar-nav d-flex align-items-end me-auto">
                <% if (isAuthenticatedUser) { %>
                    <!-- 'isAuthenticatedUser' is assigned to res.locals with Passport's 'req.user' in app.js -->
                    <a class="nav-link rounded-pill px-3 custom-link" href="/campgrounds">All Campgrounds</a>
                    <a class="nav-link rounded-pill px-3 custom-link" href="/campgrounds/new">New Campground</a>
                    <% } else { %>
                        <a class="nav-link rounded-pill px-3 custom-link" href="/campgrounds">All Campgrounds</a>
                        <% } %>
            </div>
            <div class="navbar-nav ms-auto d-flex align-items-end">
                <% if (isAuthenticatedUser) { %>
                    <a class="nav-link rounded-pill px-3 custom-link" href="/logout">Log Out</a>
                    <% } else { %>
                        <a class="nav-link rounded-pill px-3 custom-link" href="/login">Log In</a>
                        <a class="nav-link rounded-pill px-3 ms-2 custom-link" href="/register">Sign Up</a>
                        <% } %>
            </div>
        </div>
    </div>
</nav>